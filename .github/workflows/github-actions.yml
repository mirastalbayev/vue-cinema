name: CI/CD
on: workflow_dispatch
jobs:
  Validate:
    runs-on: ubuntu-latest
    steps:
      - name: Display some Text
        run: echo "Some Text"
      - name: Command in Multiline
        run: |
          node -v
          npm -v
